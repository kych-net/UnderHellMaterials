# 执行指令

承接 `素材/魔法.md`:魔法是长在生物组织里的神经系统,执行网络按生物神经网络工作。本文件设计执行网络上运行的"执行指令"。

## 指令的本质

执行指令不是字节码,而是一组**激活配方**:一个输入模式,一套必须满足的神经条件,一次发火,一个物理效应。

- 输入模式:触发这条指令的感知/意志特征(如语调、手势、血液中某种信号)。
- 神经条件(阈值与权重):当前供能、回路完善度、是否处于可触发状态。
- 发火:达到阈值后的整体放电,产生一次效应。
- 物理效应:对真实世界的作用(热、位移、形变、光、声等)。

## 指令的结构

| 字段 | 意义 |
|---|---|
| 触发 | 需要在输入中匹配的模式 |
| 充能 | 至少需要多少供能才可能发火 |
| 权重 | 回路成熟度;越高越稳,越低越易受干扰 |
| 效应 | 发火后输出的物理效果 |
| 损耗 | 一次触发对回路的负担;超限损伤 |
| 固化 | 已固化通路=True(无咏唱),现组=False(咏唱) |

## 指令分类(按效应)

- 位移类:移动自身或物体。
- 形变类:改变形状、软硬、聚合态。
- 传导类:热量、电流、振动。
- 光声类:发光、寂静、扩声、拟声。
- 转化类:物质在这生物能量系统中被重组。
- 遮蔽类:干扰感知回路,加装人的接收模式。
- 塑形类:作用于意志与神经,施加影响或压制。

## 三层接法

- 咏唱魔法(在线输入):现组一条通路,把触发、效应当场配起来再发火。灵活、慢、不稳。
- 无咏唱魔法(预编译):通路已固化,触发即发火。快、稳,但触发模式固定。
- 魔术(应用程序):把多条固化通路拼成复合网络,含转发与分岔,面向使用,无咏唱级别细看是黑盒。

## 复合与虚指令

- 复合指令:多条指令按序或并行组合,后一条可拿前一条的产物当输入。
- 虚指令:不直接发火,只组装/条件分派其它指令,类似函数的调度层。
- 反馈:指令正常发火会改变自身权重(练习之);失败或超载会留痕(神经疲劳或损伤)。

## 设计原则

- 一个魔法对设计者不是盲盒,报清楚每条指令的触发、条件与效应。
- 越有规律越易被破解;把破译成本算进权重复杂度与通路深度。
- 供能与损耗挂钩:供能决定最大发火强度,超载损伤回路。

---

备注:本文件为创作参考,承接 `素材/魔法.md`,不作为正文事实直接合入。如需采用需在 `文档/` 中另行整理。